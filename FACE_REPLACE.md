# face_replace.py — 双人身份替换

把参考照片里的**身份**转移到原视频演员的**表演**上。原片是动作模板，PNG 是身份模板；
不是把 PNG 贴到画面上。

## 安装

```bash
./setup_face_replace.sh
```

装 ffmpeg + `numpy / opencv-python-headless / onnxruntime / insightface`，
并下载四个模型到 `models/`（共约 1.1 GB，都从 GitHub release 拉，不经过 HuggingFace）：

| 模型 | 作用 |
| --- | --- |
| `buffalo_l/` | 人脸检测 + ArcFace 512 维身份向量 |
| `inswapper_128.onnx` | 换脸本体 |
| `xseg_1.onnx` | 遮挡感知面部 mask（手挡脸时不会盖住手） |
| `gfpgan_1.4.onnx` | 可选的面部增强，默认关闭 |

## 运行

```bash
python3 face_replace.py \
    -i input_video.mp4 \
    --person-a person_A.png \
    --person-b person_B.png \
    --models models \
    --map 1:A,0:B \
    -o final_replace.mp4
```

输出固定为：与源片同分辨率、同 FPS、同时长，H.264 / yuv420p / CRF 18 / preset slow，
音频原样复制成 AAC 并保持同步。

## 参数

| 参数 | 说明 |
| --- | --- |
| `--map` | 演员到参考图的对应，如 `1:A,0:B`。lead 编号由 pass A 打印出来（按出现次数排序） |
| `--crop` | `auto` 自动去黑边（模型只看真实画面），`none`，或 `W:H:X:Y` |
| `--sim-thr` | 一条轨迹要多像某位主角才会被替换，默认 `0.30`。调高＝更保守 |
| `--margin` | 两位主角得分差的下限，默认 `0.05`。差距不够就**整条轨迹跳过**，宁可不换也不换错 |
| `--enhance` | GFPGAN 混合比 0–1，默认 `0`（关）。见下方说明 |
| `--reuse-tracks` | 复用 pass A 的检测缓存，改 `--map` 或重渲时不用重跑检测 |
| `--max-frames` | 只处理前 N 帧，出样片用 |

## 身份为什么不会 A/B 对调

这是整套流程里唯一不能出错的地方，所以判定**不在帧上做，在轨迹上做**：

1. **pass A** 逐帧检测人脸，每张脸取一个 512 维 ArcFace 向量。
2. 按 IoU 把跨帧的检测连成**轨迹**（允许最多 8 帧的短暂丢失）。
3. 自动找出片中出现最多的两位主角，算出两个身份中心向量。
4. 每条轨迹用它**全部检测的平均向量**跟两个中心比一次，**一条轨迹只判一次**。
   所以一条轨迹从生到死都是同一个人——中途不可能翻。
5. 相似度低于 `--sim-thr`，或者两位主角得分差小于 `--margin`（侧脸、糊帧、背对镜头
   容易出现），这条轨迹**整条跳过**，保留原样。路人甲乙同理，永远不会被替换。

## 遮挡

`inswapper` 自己的贴回逻辑会把脸直接盖上去——手从脸前经过时手会被糊掉。
这里改成用 XSeg 出的遮挡感知 mask 贴回：XSeg 只把**真正露出来的皮肤**判为面部，
手、麦克风、头发遮住的部分自动排除，所以手仍然在脸前面。

贴回前还做一次 LAB 空间的均值/方差匹配，把原片的光线、色温和调色套到换上去的脸上。

## 关于 `--enhance`

GFPGAN 能把 128×128 的换脸输出补锐，但有两个代价：

- **闪烁**。它是逐帧幻觉细节，相邻帧补出来的纹理不一样，动起来脸会抖——
  正好违背"不允许逐帧改变长相"。
- **速度**。CPU 上 2.63 秒/张脸。全片两个人约 2600 张脸，等于多花两个多小时。

原片本身是 864 宽的软画质、带颗粒，换上去的脸不锐反而更容易融进去。
所以默认关闭；真要用建议 `--enhance 0.4` 左右，不要给满。

## 这套方案的边界

换的是**面部区域**。爆炸头假发、发际线、头骨轮廓、身体、服装、粉色羽毛围巾
全部是原片演员的——这也正是"身体比例必须跟原视频一致"想要的效果。

参考照片里两位本来就戴了同款爆炸头、粉色羽毛围巾和黑衣服，服装在拍摄阶段就已经对上了，
所以只换脸的成品会非常接近"整个人被换掉"的观感。

真正连发型、体型、服装一起重新生成，需要视频生成模型（Runway Act-Two、
可灵多图参考，或本地 GPU 跑 WAN-VACE 这类），不是 FFmpeg/OpenCV 能做的事。
